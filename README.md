# IaC-Terraform-for-vpc-eks

Run below command for kube_config: 
aws eks update-kubeconfig --region us-east-1 --name my-eks-cluster
To view current k8s cluster selected: 
kubectl config current-context